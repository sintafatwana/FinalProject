name: Web and API Automation Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # Download repo
      - uses: actions/checkout@v4

      # Setup Java
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Install Chrome browser
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2.1.0

      # Make gradlew executable
      - name: Setup gradlew
        run: chmod +x gradlew

      # Execute Gradle command to run Cucumber tests
      - name: Run Web Cucumber Tests
        run: ./gradlew webCucumber

      # Run API Tests
      - name: Run API Cucumber Tests
        run: ./gradlew apiCucumber

      - name: Archive Web Report
        uses: actions/upload-artifact@v4
        with:
         name: Web Cucumber Report
         path: reports/web-report.html

      - name: Archive API Report
        uses: actions/upload-artifact@v4
        with:
         name: API Cucumber Report
         path: reports/api-report.html

      # Deploy report to GitHub Pages
      - name: Deploy report to Github Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4.0.0
        with:
          personal_token: ${{ secrets.GITHUB_TOKEN }}
          PUBLISH_DIR: reports

      - name: Generate Allure Report
        run: |
          curl -o allure.zip -L https://github.com/allure-framework/allure2/releases/download/2.24.0/allure-2.24.0.zip
          unzip allure.zip -d allure
          export PATH=$PATH:$(pwd)/allure/bin
          allure generate build/allure-results --clean -o build/allure-report
          
      - name: Upload Report as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: build/allure-report
